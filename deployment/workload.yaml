---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nagiosserver
spec:
  selector:
    matchLabels:
      app: nagiosserver
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: nagiosserver
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - env:
        - name: site
          value: support
        image: registry.support.tools/nagios/server:BUILD_NUMBER
        imagePullPolicy: IfNotPresent
        name: nagiosserver
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: 80
            scheme: HTTP
          periodSeconds: 2
          successThreshold: 1
          timeoutSeconds: 2
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: 80
            scheme: HTTP
          periodSeconds: 2
          successThreshold: 2
          timeoutSeconds: 2
